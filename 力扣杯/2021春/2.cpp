#include <bits/stdc++.h>
#include "LCStructInput.h"
#include "debug.h"
using namespace std;

int main() {
    freopen("in.txt", "rb", stdin);
    // LCFormat lc;
    // vector<int> v;
    // lc.read(v);
    // ListNode* head = lc.readList();
    // TreeNode* root = lc.readTree();
    // lc.print(head);
    // lc.print(root);
    // lc.preOrder(root);
    // lc.inOrder(root);
    // lc.postOrder(root);

    return 0;
}